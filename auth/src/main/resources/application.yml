server:
  port: 8081

spring:
  application:
    name: auth

  datasource:
    url: jdbc:mysql://localhost:3306/auth
    username: root
    password: hSelMan%231?!
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  main:
    allow-circular-references: true
