server:
  port: 8761
spring:
  application:
    name: eureka
eureka:
  instance:
    preferIpAddress: true
    hostname: eureka
  client:
    fetchRegistry: false
    registerWithEureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka/

